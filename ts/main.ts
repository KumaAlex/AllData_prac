interface User {
    firstName: string
    lastName: string
    role: string
}

const user: User = {
    firstName: "Angela",
    lastName: "Davis",
    role: "Professor",
  }
   
  console.log(user.firstName)